{{- if .Values.injector.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-koshi-webhook
spec:
  type: ClusterIP
  ports:
    - port: 443
      targetPort: {{ .Values.injector.port }}
      protocol: TCP
  selector:
    app: koshi-injector
{{- end }}
